module.exports = exported;
//or you can write just the function by itself without the obj
const exported =
{ 
    add : function (x,y) {
        return x+y;
    },

    sub: function (x,y) {
        return (x-y); 
    }
}
